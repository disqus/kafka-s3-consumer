package kafka.s3.consumer;

message Message {
  // required int64 timestamp = 1;
  // optional string partition = 2;

  // S3 path for uploaded files.
  required string path = 1;
  required string data = 2;
}
